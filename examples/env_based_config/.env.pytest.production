# Production environment configuration
# Use with: ENV=production pytest

X_VERBOSITY=0
X_LOG_CLI=false
X_LOG_LEVEL=WARNING

# Only run smoke tests in production
X_MARKERS=smoke

# No coverage in production
X_COVERAGE_ENABLED=false
